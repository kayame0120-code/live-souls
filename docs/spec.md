# 現場手帳 仕様書 v2.4 (2026-07-11)

## 実装者への指示(削除禁止)
本書に明記されていない仕様を実装者が独自に決定することを禁止する。
`[未決]` の項目および本書にない仕様は実装せず、質問として片倉に返すこと。
**「既存の型を踏襲する」とだけ書いて実カラムを書いていない項目は許さない。
[確定]なら本文に具体カラムまで書く。書かれていなければそれは[未決]である。**

ステータス凡例: `[確定]`=変更禁止 / `[既定]`=デフォルト値・変更は要確認 / `[未決]`=実装禁止

**本書はv2.3の`member-colors_STARTO_v0_1.md`由来の記述に事実誤認があった件を、
片倉さん本人(実際のファン)の訂正を受けて改めた版である。
v2.0-draft/v2.1/v2.2/v2.3を本書で置き換える。**

---

## 0. 準拠するプロジェクト規約
- 認証は **Fortify限定**(Breeze禁止)。登録は招待制。
- DBは **PostgreSQL** / デプロイ **Fly.io(nrt)**。
- 機密情報(FCログインID・パスワード・メール・電話・住所)は `encrypted` キャストで暗号化保存。
- **マルチテナント**(全テーブル `user_id` スコープ・他人のデータは403)。ただし `attendance_photos` は例外(規約0-6の例外②、閲覧はメンバー間共有・書込削除は投稿者のみPolicy)。
- 状態は **ENUM禁止**、`string` + バリデーションで表現。
- 申込/当選履歴・当選率は **pivot(attendance_identity)から集計**。手入力させない。
- 年齢は `birth_date` から自動計算(年齢カラムを持たない)。
- 参戦写真の保存先: ローカル開発=`local`ディスク / 本番=`s3`ディスク(Fly Tigris・S3互換、`PHOTO_DISK`環境変数で切替)。実装済み。
- **LLM連携は`LlmService`インターフェースで抽象化し、`LLM_DRIVER`環境変数(`ollama`/`openai`等)で切替する**（v2.1で修正。旧「Ollama一本化」記述は撤回。理由・詳細は4.1）。

## 0.1 既存の土台(current_code.html実物・2026-07-11時点で再確認)
| テーブル/機能 | 実装状況 |
|---|---|
| `identity_groups`(id, user_id, name, sort_order) | 実装済み。片倉さんが自分で作るFCグループ(例:「LUMIÈRE FC」)。**アイドルグループ本体とは別概念**(3章参照)。変更不要 |
| `persons`(id, user_id, name, birth_date, phone[enc], address[enc], label) | 実装済み。個人情報はここに1本化 |
| `fc_memberships`(id, user_id, person_id, group_id, artist_name, member_no, login_id[enc], email[enc], password[enc], joined_on, oshi_color) | 実装済み |
| `venues` / `venue_notes` | 実装済み。会場単位 |
| `venues.arena_view_key`(nullable string) | **マイグレーション済み**(REPORT.md V1確認)。利用側(360度ビュータブ)は未実装 |
| `events`(id, tour_id[FK・NOT NULL], event_label[nullable], venue_id[nullable], event_date, start_time, timestamps) | v1.4で再編済み。旧`event_name`はDROP済み。**公演名は`tours.name`＋`event_label`で解決する** |
| `tours`(id, name, timestamps) | **v1.4で実装済み**。単発公演も「1公演だけのツアー」として1件作る設計。全ユーザー共有マスタ(user_idなし)。**ツアー束ねの仕組みは既にこれ** |
| `attendances`(id, user_id, event_id[FK], open_time, start_time, seat_raw/block/row/number, status, companion, memo) | 実装済み。旧`event_name`/`event_date`/`venue_id`はDROP済み(event_id経由に統一)。`open_time`/`start_time`は物理削除せず**個人の手動上書き・メモ扱いとして温存**(QV13-1) |
| `attendance_identity`(pivot: attendance_id, fc_membership_id, result, ticket_count) | 実装済み |
| `attendance_photos` | 実装済み。EXIF/GPS除去済み保存、5枚/参戦上限。**heicは現状拒否**(libheif未導入・QV12-1、安全側仮置き。infra課題であり本書の対象外) |
| `FcMembership::expiryDate()`/`renewalWindowStart()`/`isInRenewalWindow()` | 実装・テスト済み |
| グループ削除ガード(E1) | 実装・テスト済み(A案=配下0件時のみ削除) |
| `EventImportParser` | **未削除**。後継のAI一括登録(LlmService経由)が未実装のため、§5合意まで保留中(規律通り) |
| `LotImportService` | 未使用化(廃止済み扱い) |
| ホーム更新通知・チケット確認・開場時間表示 | **v2.0で実装・テスト済み**(REPORT.md T1〜T3、118テスト全通過)。チケット確認閾値=7日はCC裁量(`HomeService::getTicketReminders()`の`addDays(7)`、変更可) |
| Google Places API(会場オートフィル) | 実装済み |

## 1. 概要と目的
複数のFC名義を持つヲタクが、名義管理・参戦記録・当落管理・現場ロジ・写真・セットリストまでを1本のアプリに集約するv2改修。既存実装(0.1)を土台に、モックで具体化されたユーザーストーリー由来の新規機能を追加する。

## 2. 変更区分サマリー

### 2.1 変更なし(既存実装をそのまま活用)
| 項目 | 根拠 |
|---|---|
| グループ管理(追加/編集/削除/並び替え) | `identity_groups`で完結 |
| 名義管理・FC情報管理 | `fc_membership`が名義情報そのもの。統合済み |
| 申込管理・当落・参戦管理 | `attendances`/`attendance_identity`フローで完結 |
| 公演一覧・公演詳細の基盤 | `events`/`tours`で完結。**ツアー束ねも`tours`が既に担っている**(v2.0-draftのC3案=新規カラムは撤回) |
| 更新周期の判定 | `joined_on`からの自動計算を維持 |
| 同行者情報 | `attendances.companion`の単一自由記述欄のまま |
| 現場詳細の交通・宿泊 | `venue_notes`(会場単位)のまま |
| 参戦記録のファンサ情報 | `attendances.memo`で十分 |
| 座席・写真のアップロード/削除/共有閲覧 | 既存実装で完結 |
| 写真の本番保存先 | Fly Tigris(S3互換)で対応済み |

### 2.2 新規実装(スコープ確定)
| 項目 | 内容 | ステータス |
|---|---|---|
| ホーム画面の更新通知 | `isInRenewalWindow()`該当名義をホームにカード表示 | `[確定・実装済み]` |
| チケット確認の一言 | 公演日が近い`planned`予定に受動表示 | `[確定・実装済み]` |
| 開場時間の表示 | `attendances.open_time`を参戦詳細に表示 | `[確定・実装済み]` |
| ナビゲーション構造 | 上部pill型・**5タブ**(ホーム/参戦記録/名義/当落/公演)。公演をメインタブに格上げ | `[確定]`(v2.1で明記。実装は既に5タブ・QV13-6) |
| 名義複製 | 既存`person`を選び、別`identity_group`配下に新規`fc_membership`を作成。**画面設計も確定**：名義詳細に複製ボタン→複製画面遷移。個人情報(住所/電話/メール/誕生日)は入力させず「引き継ぎ」表示。名義固有情報のみ入力 | `[確定]`(v2.1でHANDOFF§3.2承認) |
| 担当メンバーマスタ | 新規共有マスタ`idol_groups`(全ユーザー共有・user_idなし)を新設し、その配下に`group_members`(公式メンバー×公式カラー)を置く。**選択方式は「公式選択主・自由色従」で確定**：①アイドルグループ選択→②公式メンバーが色付きで並ぶ→③選択で担当色が自動反映→④「手動で変える」で11色スウォッチから上書き可。既存`oshi_color`は**「自動反映された公式色を格納しつつ上書き可能」なカラムとして意味変更**して存続。**データ源は`member-colors_STARTO_v0_1.md`をリポジトリに配置し、CCが直接シーダー化する方式で確定**(3章参照) | `[確定]`(v2.3でカラム確定・7章から除去) |
| AI公演一括登録 | 貼付→**LlmService**(環境変数`LLM_DRIVER`で`ollama`/`openai`切替)→確認画面(DBに一時テーブルは持たない・既存`EventImportParser`と同型のステートレス方式)→登録。`EventImportParser`は廃止予定(未実装のため未削除) | `[確定]`(v2.2で確認方式を訂正。詳細は4.1・3章) |
| AIセットリスト一括登録 | 公演情報と同一のLlmServiceパイプラインを共用。**UI入口は公演用/セトリ用で分離**(公演一覧「一覧を貼って一括登録」／公演詳細→セットリスト画面「セトリを貼って一括登録」) | `[確定]`(v2.1でHANDOFF§3.5承認) |
| AI当落締切一括登録 | 貼付→LlmService→確認画面(会場名+日付で該当`events`行を自動マッチ、未マッチは手動選択)→`events.application_deadline`/`announce_date`を更新。**手入力との両刀**(会場・公演情報とは別ページに載っていることが多いため専用の入口を持つ) | `[確定]`(v2.2で新規追加。詳細は4.1・4.6) |
| 名義一覧のFCタブ束ね | 名義一覧を`identity_group`(FC)タブで束ね、タブ切替で該当グループの名義カードを表示 | `[確定]`(v2.1でHANDOFF§3.3承認) |
| 公演・当落の2階層化 | 公演タブ：公演一覧(ツアー名カード=`tours`)→公演詳細(そのツアー配下の全`events`を日程行で表示)。当落タブ：申込一覧(ツアー名カード)→当落詳細(名義×枚数×当落セレクト)。**束ね単位はツアー＝既存`tours`をそのまま使用。新規カラムは不要**(下記参照) | `[確定]`(v2.1でHANDOFF§3.4承認、ツアー束ねの実装方式を修正) |
| 現場詳細:チケット確認通知 | (2.2冒頭と同一・実装済み) | `[確定・実装済み]` |
| 写真管理:360度ビュー連携 | 「見え方マッピング」と内部タブ併存(タブ切替)で確定。`venues.arena_view_key`(実装済み・nullable)で別プロジェクトのHTML資産と紐付け。**タブUI自体はDeploy1に含める。実データ連携・埋め込みはDeploy2(9章)** | `[確定・方針]`、マージ方法は`[未決]`(7章No.1・実装時にプロトタイプして判断) |
| セットリスト管理 | 公演登録(events/tours)と同型の設計。カラムは3章で確定 | `[確定]`(v2.2でカラム確定) |

### 2.3 廃止
| 項目 | 理由 |
|---|---|
| `EventImportParser`とそのテスト | LlmService一本化の決定による。後継未実装のため物理削除はまだ行わず、実装完了後に削除する(規律通り) |

## 3. データ(テーブル定義・新規分のみ)
既存テーブル(0.1参照)は変更なし。新規は名称・関係のみ確定、カラム定義は設計フェーズで詰める。

| テーブル(仮称) | 用途 | 関係 | ステータス |
|---|---|---|---|
| `idol_groups`(仮称) | 実在アイドルグループのマスタ(Snow Man等)。カラム：`id` / `name`(string, NOT NULL) / `status`(string, nullable。値は`null`=現役／`"休止"`／`"解散"`。ENUM禁止のためstring) / `timestamps`。**全ユーザー共有・user_idを持たない**(`venues`/`events`/`tours`と同型) | 独立の新規共有マスタ | `[確定]`(v2.3でカラム確定) |
| `group_members`(仮称) | 公式メンバー×公式カラーのマスタ。カラム：`id` / `idol_group_id`(FK→idol_groups, NOT NULL) / `name`(string, NOT NULL) / `color_name`(string, NOT NULL・出典の色名をそのまま格納。例:"黄色") / `color_hex`(string, nullable・実装時に色名から変換。座席座標の独自算出と同じ「それらしければよい」精度方針でよく、公式の精密な値である必要はない) / `source_type`(string, NOT NULL・"公式"/"ファン共通認識") / `sort_order`(integer) / `timestamps` | `idol_groups` 1 : N `group_members` (**`identity_group`配下ではない**。v2.0-draftの記述は誤りだったため訂正) | `[確定]`(v2.3でカラム確定) |
| `fc_memberships.group_member_id`(仮称カラム) | どの公式メンバーを担当しているかの参照。`oshi_color`はここから自動反映された色(`color_hex`)を格納し、上書き時のみ独自値に置き換わる | `fc_memberships`に追加(FK→group_members, nullable) | `[確定]`(v2.3でカラム確定) |
| `identity_groups`への変更 | 検討したが**変更不要**。`idol_groups`とは独立の概念のまま(FC名義グループと実在アイドルグループの紐付けは行わない) | — | `[確定]` |
| `tours`(既存) | ツアーの束ね単位。**新規カラム不要**。公演一覧・当落一覧はこの`tours`をカード単位として表示し、詳細画面で配下`events`を列挙する | 既存のまま(0.1参照) | `[確定]`(v2.1でC3案=新規カラムを撤回) |
| `events.application_deadline`(仮称カラム・datetime, nullable) | 申込締切日時 | `events`に追加 | `[確定]`(v2.2で追加・下記コラム参照) |
| `events.announce_date`(仮称カラム・date, nullable) | 当落発表日 | `events`に追加 | `[確定]`(v2.2で追加・下記コラム参照) |
| `setlists` | 公演のセットリスト本体。カラム：`id` / `event_id`(FK→events, **unique**, NOT NULL) / `timestamps` | `events` **1 : 1** `setlists`(1公演=1セットリストで確定。共有マスタ、user_idスコープなし) | `[確定]`(v2.2でカラム確定) |
| `setlist_items` | セットリストの曲目。カラム：`id` / `setlist_id`(FK→setlists, NOT NULL) / `sort_order`(integer, NOT NULL・表示順) / `display_label`(string, nullable・"01"/"EN"等のラベル。未入力ならUIが`sort_order`から自動採番) / `title`(string, NOT NULL) / `timestamps` | `setlists` 1 : N `setlist_items` | `[確定]`(v2.2でカラム確定) |
| `venues.arena_view_key`(既存・実装済み) | 360度ビューエンジンの資産と紐付ける参照キー | `venues`に追加済み | `[確定・実装済み]`、マージ方法は`[未決]` |
| AI一括登録の確認画面(**テーブルではない**) | LlmService出力の確認・編集・登録用。**既存`EventImportParser`と同じくDBに一時テーブルは持たない**：パース結果をそのまま編集可能なHTMLフォームへ渡し、確認送信時にフォーム値を直接INSERT(`events.import.parse`→`events.import.store`と同型)。公演・セットリスト・締切の3系統とも同方式 | ステートレス(永続化なし) | `[確定]`(v2.2で「人間確認テーブル」という誤解を招く表現を訂正) |

### 3.1 申込締切・当落発表日の所在について(`tours`ではなく`events`にした理由)
片倉さんの指示は「`tours`テーブルのカラムに追加する」だったが、**`events`に追加する形で確定させた**。
理由：改修版`mockup.html`の当落詳細画面(`#scr-lot-detail`)で、同一ツアー「LUMIÈRE「Prism of Night」」配下の
`events`ごとに異なる締切/発表日が表示されている(京セラドーム大阪＝締切07-20・発表07-28、東京ドーム昼公演＝発表予定07-15、
横浜アリーナ＝発表済み06-20で結果確定)。1つの`tours`行にカラムを持たせると、同一ツアー内で会場ごとに違う締切を
表現できない(値が1つしか持てない)。締切/発表日は「どの日程(=`events`)に申し込むか」に紐づく情報のため、
`events`側に置くのが技術的に正しい。**「片倉さんの指示と実際に作ったモックの中身が矛盾している」ケースなので、
黙って指示通りにもせず、黙って上書きもせず、ここに明記する。** 単発公演(1ツアー=1公演)の場合は`events`1行に
締切が乗るだけなので、`tours`に置いた場合と見た目の結果は変わらない。この判断に異論があれば差し戻してほしい。

### 3.2 `group_members`のデータ投入方式・NEWSカラーの訂正について
**v2.3で「NEWSは1人2色体制、実在確認済み」と書いたのは誤り。訂正する。** 投入元
`member-colors_STARTO_v0_1.md`は当該項を「出典区分:公式」としていたが、片倉さん本人(実際に毎年NEWSに現場参加している当事者)
の訂正により、正しくは以下の**単色**で確定する：

| メンバー | カラー(訂正後) |
|---|---|
| 小山慶一郎 | 紫 |
| 加藤シゲアキ | 緑 |
| 増田貴久 | 黄 |

`member-colors_STARTO_v0_1.md`のNEWSの項(「2022年以降は1人2色体制」の記述含む)は誤りとして扱い、
CCへのデータ投入時は本書のこの表を正として使うこと(mdファイル側の当該行は使わない)。
**「複数色対応」の設計は不要になった**。`group_members.color_name`は常に単色の前提でよく、
`oshi_color`自動反映時の「先頭の色を採用」ルール(v2.3で追加)も撤回する(単に`color_hex`をそのまま使う)。
カラム自体(`color_name`が単なるstring)は変更不要——複数色を想定した特別扱いが要らなくなっただけ。

**参考**：`member-colors_STARTO_v0_1.md`は「出典区分:公式」の項目でも誤りを含んでいたことが今回判明した。
CCが機械的に信じて投入する前提のファイルなので、片倉さんが特に詳しいグループだけでも投入前に軽く見返すことを推奨する
(全件チェックしろという話ではない。今回のように「これは違う」と分かるものがあれば都度本書へ訂正を反映すればよい)。

**データ投入方式**：`member-colors_STARTO_v0_1.md`をリポジトリの`docs/`配下に配置し、CCがこのファイルを直接読んで
`idol_groups`/`group_members`のシーダーを実装する(設計フェーズを別途挟まず、ファイル→実装を直結させる)。
STARTO以外(TOBE/EBiDAN)や、ファイル内で保留扱いのグループ(King & Prince・Aぇ! group・20th Century・TOKIO)は、
同形式の追加mdファイルを投入すればそのまま拡張できる設計とする。`color_hex`(実際の表示色)はmdに無いため、
CCが色名から実装時に変換する(前述の通り精密な公式値である必要はない)。

## 4. ロジック・処理フロー

### 4.1 AI一括登録(公演情報・セットリスト・当落締切の3系統共通)【v2.2で方式修正】
1. ユーザーが対象のテキスト(公演情報／セットリスト／当落締切のいずれか)を貼り付け
2. Laravelアプリが入力を受け付け、`LlmService`インターフェース経由でLLMへ送信
3. LLMがテキストをJSON化して返却
4. **JSONをそのまま編集可能なHTMLフォームへ渡し確認画面を表示する(DBへの一時保存はしない)**。
   既存`EventImportParser`→`events.import.parse`/`events.import.store`と同じステートレス構造を
   セットリスト・当落締切の2系統にもそのまま適用する(3章参照)
5. 確認画面で内容を修正・除外行のチェック解除・(当落締切のみ)対象`events`の手動選択を行い、送信でDB登録

**アーキテクチャ(`[確定]`・QV20-6の決定を反映。旧v2.0-draftの「同一Flyマシン上でOllama同居」は撤回)**:
- Flyマシンの現行スペック(1GB RAM/shared CPU 1コア)ではOllama同居はOOM確実と実証済み。片倉の判断(追加課金なし)によりC案で確定
- `LlmService`インターフェースで抽象化し、`LLM_DRIVER`環境変数で切替：
  - ローカル開発 = `ollama`(同一マシン内)
  - 本番 = クラウドLLM(`openai`等)。APIキー(`OPENAI_API_KEY`等)は人間が`.env`にセット
- キュー(queue worker)経由で実行し、Web側リクエストと資源を奪い合わない(既定のまま維持)

### 4.2 更新通知(ホーム画面)【実装済み】
既存`FcMembership::isInRenewalWindow()`を呼び出し、該当する名義をホームにカード表示する。

### 4.3 チケット確認通知【実装済み】
公演日が近づいた`planned`の参戦予定に対し「チケット確認はお済みですか?」を一言添える。閾値=7日(CC裁量、`addDays(7)`一箇所で変更可)。

### 4.4 events.start_time の保存方式【v2.1で追記・要合意事項の反映】
`events.start_time`は`time`型カラム。**Eloquentの`'datetime:H:i'`キャストは使用しない**(PostgreSQLでは`time`列に`Y-m-d H:i:s`形式を書き込もうとして本番INSERTが失敗する。`EventDuplicateKeyTest`のFAILで実証済み)。
**アクセサ/ミューテータ方式で確定**(`[確定]`)：取得時=Carbonへ変換、保存時=`H:i:s`文字列へ正規化。重複判定(`EventService::findDuplicates`)も`H:i:s`正規化後に比較する。

### 4.5 名義複製ロジック【v2.1で追記】
複製元の`person`を選択 → 複製先`identity_group`を選択 → 担当メンバー選択(グループ→メンバー→色自動反映、上書き可) → FC固有情報(会員番号・パスワード・ID・入会日等)を新規入力 → 保存時は既存`person`のIDを参照して新規`fc_membership`を作成(`persons`は重複させない)。

### 4.6 AI当落締切一括登録フロー【v2.2で新設】
公演情報(会場・日付・開演)と当落締切(締切日時・発表日)は、公式サイト上で**別ページに分かれていることが多い**ため、
公演一括登録とは別の専用入口を持つ(`[確定]`・2.2参照)。1回のコピペで両方を漏れなく拾わせようとしない。
1. ユーザーが締切情報のテキスト(複数会場分の締切一覧など、長文貼り付け可)を貼り付け
2. `LlmService`が各行を「会場名・日付・締切日時・発表日」に構造化してJSON抽出
3. 確認画面で各行に対し、会場名+日付から一致する既存`events`を自動マッチして候補表示する。**一致しない行は対象`events`を手動選択させる**(該当がまだ登録されていなければ、先に公演一括登録を案内)
4. 送信で該当`events`の`application_deadline`/`announce_date`を更新
5. **抜け漏れは手入力で救う**：一括登録で拾いきれなかった/間違えた締切は、公演詳細または当落詳細の編集画面から直接手入力・修正できる(AI一括登録は必須動線ではなく時短手段の一つという位置づけ)
6. 締切超過の判定は**サーバー側**で行う(`now() >= application_deadline`)。端末時計に依存しない(`[既定]`)

## 5. 画面(新規/変更分のみ)
| 画面 | 内容 | ステータス |
|---|---|---|
| ナビゲーション | 上部pill型・5タブ(ホーム/参戦記録/名義/当落/公演) | `[確定]` |
| 名義複製 | 名義詳細の複製ボタン→複製画面。個人情報は「引き継ぎ」表示・入力スキップ。名義固有情報のみ入力 | `[確定]` |
| 担当メンバー選択 | 名義編集/複製画面内。グループ選択→公式メンバーが色付きチップで並ぶ→選択で担当色自動反映→「手動で変える」で11色スウォッチ上書き | `[確定]` |
| 名義一覧 | FCタブ(`identity_group`)で束ね、タブ切替で該当名義カードを表示 | `[確定]` |
| ホーム | 「次の現場」「確認が必要」「申込締切」「更新期間の名義」「積み上げ」「直近の記録」 | `[確定・実装済み]`(申込締切カードは`events.application_deadline`/`announce_date`確定により表示内容も確定) |
| 参戦詳細 | 公演情報(共有マスタ)・座席記録・メモ・写真に加え、開場/開演を別行で併記 | `[確定・実装済み]` |
| 会場詳細 | 「見え方マッピング」に加え、内部タブで「360度ビュー」を併存。タブUI自体はDeploy1、実データ連携はDeploy2(9章) | `[確定・方針]` |
| 公演一覧/公演詳細 | 一覧=ツアー名カード(`tours`)。詳細=そのツアー配下の全`events`を日程行で一覧表示 | `[確定]` |
| 当落一覧/当落詳細 | 一覧=申込中ツアー名カード。詳細=`events`ごとに名義×枚数×当落セレクト＋締切/発表日表示。当選率・集計は一切表示しない | `[確定]` |
| AI公演一括登録 | 公演一覧の「一覧を貼って一括登録」から遷移。貼り付け→LlmService→確認画面(DBテーブルなし)→登録 | `[確定]` |
| AI当落締切一括登録 | 当落一覧または公演詳細から遷移。貼り付け→LlmService→確認画面(該当`events`を自動マッチ／手動選択)→登録。手入力フォールバックあり(4.6) | `[確定]`(v2.2で新設) |
| セットリスト(手動/AI一括) | 公演詳細→セットリスト画面。手動登録フォーム＋「セトリを貼って一括登録」ボタン(公演登録とは別入口)。カラムは3章で確定 | `[確定]` |

## 6. エッジケース・既存未決事項
| ケース | 扱い | ステータス |
|---|---|---|
| QV13-1: 開演の真実の源 | `events.start_time`を正とする。`attendances.open_time`/`start_time`は個人の手動上書き・メモ扱い | `[確定・実装済み]` |
| events.start_timeの保存方式 | `datetime:H:i`キャスト禁止。アクセサ/ミューテータ方式(4.4参照) | `[確定]` |
| E1: グループ削除時に配下名義が残る場合 | A案(削除拒否)で実装・テスト済み | `[確定・既存]` |
| 名義複製時の`persons`重複登録 | 重複させず既存`persons`を参照する | `[確定]` |
| ツアー束ねの実装方式 | 新規カラムを追加せず、既存`tours`/`events.tour_id`をそのまま使う | `[確定]`(v2.1でC3案を撤回・修正) |
| Ollamaのアーキテクチャ | 同一マシン同居は不可(OOM実証済み)。`LlmService`抽象化＋環境変数切替(4.1参照) | `[確定]`(v2.1で修正) |
| 申込締切・当落発表日カラムの所在 | `tours`ではなく`events`に追加(3.1のコラム参照。片倉指示との相違点を明記済み) | `[確定]`(v2.2) |
| 申込締切の入力経路 | LlmServiceによる抽出(専用入口・4.6)＋手入力の両対応。片方に依存しない | `[確定]`(v2.2) |
| 締切超過の判定 | サーバー側で`now()`比較。端末時計は信用しない | `[既定]`(v2.2) |
| AI一括登録の確認方式 | DBに一時テーブルを持たない。既存`EventImportParser`のステートレス方式を公演・セトリ・締切の3系統で踏襲 | `[確定]`(v2.2で明記) |
| 担当メンバーマスタの公式情報スクレイピング | アーティスト公式プロフィール等からのデータ収集が、確定スコープ外の「FCページスクレイピング」(会員ページ限定)と抵触しないか設計時に線引きする | `[未決・設計時確認]` |
| `idol_groups`の休止/解散ステータス | `status`カラム(nullable string)で保持。名義編集の選択肢に出すかどうかはUI側の軽微な判断としてCCに委ねる(除外せず選べるようにするのが無難) | `[確定]`(v2.3・カラムは3章で確定) |
| `group_members`の複数色対応 | v2.3で「NEWSで実在確認済み」としたのは誤り。片倉さん本人の訂正によりNEWSも単色(3.2参照)。複数色ケースは現状確認されておらず、特別扱い不要 | `[確定・訂正]`(v2.4) |
| heic写真の受付 | libheif未導入のため現状拒否のまま(jpeg/png/webpのみ)。infra課題であり本書のスコープ外 | 参考記載(実装は安全側仮置き済み) |

## 7. 未決事項(実装禁止)
v2.2でNo.3(setlists詳細カラム)・No.4(申込締切の所在/入力経路)・No.5(AI確認テーブルのカラム)、
v2.3でNo.1(idol_groups/group_membersの詳細カラム)を、「型を踏襲する」止まりだった記述から
実カラム・実フローまで書き切ったため`[確定]`に格上げし、本章から除去した(3章・4章参照)。
残るのは技術的に「作ってみないと判断できない」性質の1件のみ。

| No | 未決の内容 | 決めるための二択・三択 |
|---|---|---|
| 1 | `venues.arena_view_key`のマージ方法(別プロジェクトのHTML資産の取り込み手順) | 片倉方針(2026-07-11)：抽象論では決められないため、arena-viewエンジン(v3)が完成した後、別ブランチで実際に組み込んでみてから方式を確定する(9章のDeploy2参照) |

## 8. 分野間整合性検査(抜粋)
| ペア | 照合質問 | 結果 | 対処 |
|---|---|---|---|
| データ×既存資産 | ツアー束ね(公演/当落2階層)に新規カラムが必要か | NO(`tours`/`events.tour_id`がv1.4で実装済み) | 新規カラム案(C3)を撤回。既存構造をそのまま活用する方針に修正 |
| インフラ×実装可能性 | Ollamaの同一Flyマシン同居は実現可能か | NO(1GB RAM/shared CPU 1コアでOOM確実・QV20-6実証済み) | `LlmService`抽象化＋`LLM_DRIVER`環境変数(開発=ollama/本番=クラウド)へ修正 |
| データ×本番互換性 | `events.start_time`の`datetime:H:i`キャストはPostgreSQL互換か | NO(`time`列への日付付き文字列書き込みで本番INSERT失敗・実証済み) | アクセサ/ミューテータ方式へ修正(4.4) |
| スコープ×既存確定 | spec記載のナビ構成(4タブ+公演)と実装(5タブ)は一致するか | NO | spec記述を5タブ表記へ更新(実装済みの状態に合わせる) |
| データ×既存資産 | `group_members`を`identity_group`配下にする元案は成立するか | NO(`identity_group`はuser_idスコープの個人FC管理。公式データは全ユーザー共有であるべき) | 新規共有マスタ`idol_groups`を新設し、その配下に置く方針へ修正 |
| データ×既存資産 | 名義複製・FC情報管理の新規実装は既存`persons`/`fc_memberships`の設計と矛盾しないか | NO | 既存構造(persons 1 : N fc_memberships)がそのまま両立する設計であることをコード突合せで確認済み |
| データ×モック実例 | 申込締切/発表日を`tours`カラムとして持てるか(片倉指示) | NO(改修版mockup.htmlの当落詳細で、同一ツアー内の`events`ごとに異なる締切/発表日が表示されている。1ツアー1値では表現不能) | `events`への追加に修正。指示との相違は3.1に明記の上で提示 |
| 実装パターン×新機能 | AI一括登録(公演/セトリ/締切)に新規DBテーブルが必要か | NO(既存`EventImportParser`はDB非依存のステートレス方式=パース結果を直接編集可能フォームへ渡し、送信時に直接INSERT) | 「人間確認テーブル」という表現を訂正。3系統とも同一のステートレス確認画面方式で統一 |
| スコープ×デプロイ計画 | `venues.arena_view_key`カラム(実装済み・nullable)はDeploy1に含めてよいか | YES(実データが無くても実害なし。マイグレーション済みでテストも通過済み) | Deploy1にそのまま含める(9章) |

## 9. デプロイ計画(スコープ境界)【v2.3で新設】
片倉方針(2026-07-11)：360度ビューを別ブランチに分離し、2段階デプロイとする。

```
main
 │
 ├─ ここまでの現場手帳の内容（360度ビュー以外の本書確定機能）── Deploy 1（本番投入）
 │
 └─ 360度ビュー専用ブランチ（arena-view統合用）── Deploy 2（arena-view完成後）
```

- **Deploy 1(main)**：本書(v2.3)およびモックで`[確定]`とした機能一式(360度ビューを除く)。`venues.arena_view_key`カラム自体は実データが無くても実害がないためそのまま含める(`[確定]`)。マイグレーション/`/up`/テストスイートの確認ラインを通してから本番投入する(REPORT.md V1〜V3の方式を踏襲)。
- **会場詳細の「360度ビュー」タブUI**（見え方マッピングとの内部タブ併存・プレースホルダー表示）：`arena_view_key`が空でも表示上の実害が無いため、**Deploy 1に含めてよいと判断する**(`[確定・仕様書著者判断]`。片倉からの直接指示ではないため、異論があれば差し戻すこと)。
- **Deploy 2(新規ブランチ)**：arena-viewエンジン(v3)側の会場実装が完成し、埋め込み方式(7章No.1)が決まった時点で着手。mainとは別ブランチで統合してから、そちらをデプロイする。
- ブランチ名・Git運用・CI/CDの具体手順は本書(アプリ仕様)の対象外。CC向けの作業指示(`cc_instructions`)側で定める(プロジェクトのカスタム指示による層分けを踏襲)。

## 変更履歴
- v2.4 (2026-07-11): v2.3の事実誤認を訂正。
  - NEWSの担当カラーは「1人2色体制」ではなく**単色**：小山慶一郎=紫／加藤シゲアキ=緑／増田貴久=黄。片倉さん本人(実際のファン)の訂正による
  - `member-colors_STARTO_v0_1.md`のNEWS項は誤りとして扱い、本書3.2の表を正とする(CCへの投入時はmdの当該行を使わない)
  - 「複数色対応」の設計・「先頭の色を採用」ルールは不要と判明したため撤回(3章・3.2・6章)
  - `member-colors_STARTO_v0_1.md`は「公式」表記の項目でも誤りを含み得ることが判明。片倉さんが詳しいグループは投入前の軽い見返しを推奨する旨を3.2に記載
- v2.3 (2026-07-11): 片倉さんの2点指摘を反映。
  - `idol_groups`/`group_members`のカラムを確定(3章)。データ投入は`member-colors_STARTO_v0_1.md`を配置しCCが直接シーダー化する方式で確定(「設計フェーズで検討」という先送りをやめた)
  - 複数色(1人2色)ケースはNEWS(3名全員)で実在確認済みと回答。`color_name`に原文格納、単一スウォッチは先頭の色を採用するルールで確定(3.2)
  - `venues.arena_view_key`のマージは別ブランチに分離し、Deploy1(360度ビュー以外を先行本番投入)/Deploy2(arena-view完成後に統合)の2段階とする方針を追加(9章新設)
  - 未決1件(7章)に圧縮
  - ツアー束ねは既存`tours`をそのまま拡張活用(No変更・v2.1の方針を維持確認)
  - Ollamaのアーキテクチャ変更(QV20-6のC案)はCC側決定でありClaudeチャット側は未共有だった旨を明記(0章・4.1で修正済み)
  - `events.start_time`のキャスト問題を修正(4.4で既に反映。再確認のみ)
  - `setlists`/`setlist_items`のカラムを実際に確定(3章)。`events/tours`型を踏襲する具体形として`event_id`(1:1)・`sort_order`・`display_label`・`title`を明記
  - AI一括登録の「人間確認テーブル」表現を訂正：DBテーブルを持たないステートレス方式(既存`EventImportParser`と同型)であることを3章・4章で明記
  - 申込締切・当落発表日は`events.application_deadline`/`announce_date`として確定。**片倉指示の「tours」ではなく「events」に置いた**(理由は3.1。モックの実例と矛盾するため相違点を明示)
  - 当落締切の入力経路をLLM抽出(専用入口・会場+日付でevents自動マッチ)＋手入力の両刀で確定(4.6)
  - `venues.arena_view_key`のマージ方法は「実装時にプロトタイプしてから判断する」という決め方自体を明記(7章No.2)
  - 未決2件(7章)に圧縮
- v2.1 (2026-07-11): CC側資料(`current_code.html`/`QUESTIONS.md`/`REPORT.md`)とモック改修(`HANDOFF_mock_to_spec.md`/改修版`mockup.html`)を再突合せ。
  - `idol_groups`(新規共有マスタ)を新設し`group_members`をその配下に修正(旧`identity_group`配下案は誤りだったため訂正)
  - HANDOFF§3の5決定案(担当メンバー選択・名義複製・FCタブ束ね・公演/当落2階層・AI入口分離)を承認・`[確定]`化
  - ツアー束ね(C3)は新規カラムを撤回、既存`tours`/`events.tour_id`(v1.4実装済み)をそのまま活用する方針に修正
  - AI一括登録のアーキテクチャを「Ollama一本化」から`LlmService`抽象化＋環境変数切替(QV20-6のC案)に修正
  - `events.start_time`の保存方式をキャストからアクセサ/ミューテータ方式へ訂正(QV13-5)
  - ナビゲーション構成をspec記述「4タブ+公演」から実装済みの「5タブ」表記へ修正
  - 未決5件(7章)
- v2.0-draft (2026-07-10): current_code.html・mockup.htmlとの突合せを反映した確定版。未決5件。突合せ前の初版ドラフトは本書に置き換えられ破棄。
